import { QueryValidatorDirective } from './query-validator.directive';

describe('QueryValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new QueryValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
